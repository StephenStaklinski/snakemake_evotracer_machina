# set path to the fastq files for a single mouse
fastqDirs:
    - /test1/path/fastqs
    - /test2/path/fastqs
    
# rule runEvotracer
fastqDirPrefix: fastq_raw

# rule runMachina
primaryTissue: BDR
batches: 1
threads: 100
